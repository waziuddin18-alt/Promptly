// Promptly — legacy data file, no longer used.
// All prompts + categories now live in Firebase Firestore
// (see firebase-config.js and app.js).
// This file is intentionally left as a no-op so that older
// cached versions of index.html don't 404 on it.
